// use jobitmap::{ByteBufferResource, ByteOperat, BaseResource};

fn main() {
//     let mut size: u64 = u32::MAX.into();
//     size += 1;
//     let last_index = size -1;
//     let test: usize = size.try_into().unwrap();
//     let br = ByteBufferResource::new("/tmp/test".to_string(), false);
//     let mut bf = br.get_content();
//     bf.put_byte(last_index.try_into().unwrap(), 255);
//     for i in 0..test {
//         bf.put_byte(i, 255);
//     }
//     assert_eq!(255, bf.get_byte(last_index.try_into().unwrap()));
}